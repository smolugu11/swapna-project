import re
# Regular Expressions (RegEx) in Python
# Regular expressions are a powerful tool for matching patterns in text. They allow you to search, match, and manipulate strings based on specific patterns.
def main():
     text ='dog'
     results =re.match('dog',text)
     print(results)
main()
